'use client'
import { createTheme } from '@mui/material/styles'

const theme = createTheme({
  palette: {
    primary: {
      main: '#1E3F85',
    },
    secondary: {
      main: '#000000',
    },
    white: {
      main: '#F3F7FA',
    },
  },
})

export default theme
